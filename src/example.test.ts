describe('Тестируем сложение', () => {
    test('Складываем 1 + 1 должны получить 2', () => {
        expect(1 + 1).toBe(2);
    });
    test('Складываем 1 + 2 должны получить 3', () => {
        expect(1 + 2).toBe(3);
    });
});